node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Secret credentials — defense in depth alongside .claude/settings.json secret-block hook
service-account*.json
serviceAccount*.json
firebase-adminsdk*.json
*.pem
*.p12
*.pfx
*.key
playwright-report/
test-results/
qa-final/
qa-shots/
output/
.playwright-cli/

# Playwright MCP session cache (auto-generated per session)
.playwright-mcp/

# Lighthouse CI reports (auto-generated)
.lighthouseci/

# Obsidian local user config + untitled canvas drafts
.obsidian/
docs/Senza nome*

# Local audit screenshots (regenerated on demand)
audit-*.jpeg
audit-*.png
.vercel

# AI tool memory/session cache
.remember/

# Claude Code per-machine local overrides (never commit)
.claude/settings.local.json

# Build artifacts and temp
metadata.json
