{
  "name": "travelliniwithus-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "description": "Layer /api/* di travelliniwithus.it. Firebase Hosting serve solo file statici: senza questa function ogni chiamata a /api/... riceve lo shell HTML della SPA. Vedi docs/14_Bugs/BUG_API_ENDPOINTS_SENZA_BACKEND_IN_PROD_2026-07-26.md",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=lib/index.js --external:firebase-functions --external:firebase-admin --banner:js=\"import{createRequire as __cr}from'module';const require=__cr(import.meta.url);\"",
    "serve": "npm run build && firebase emulators:start --only functions,hosting"
  },
  "dependencies": {
    "firebase-functions": "^7.3.2-rc.0"
  }
}
